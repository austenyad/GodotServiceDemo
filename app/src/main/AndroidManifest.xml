<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GodotServiceDemo">

        <!-- 通过 meta-data 声明插件，引擎初始化时自动加载 -->
        <meta-data
            android:name="org.godotengine.plugin.v2.AppPlugin"
            android:value="com.yad.godotservicedemo.AppPlugin" />

        <activity
            android:name=".StartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".HostGameActivity"
            android:exported="false" />


        <service
            android:name="org.godotengine.godot.service.GodotService"
            android:exported="false"
            android:process=":godot" />
    </application>

</manifest>